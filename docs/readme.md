# Writing the documentation

Run once from repository root:
```shell
./scripts/install-mdbook.sh
```

Run from repository root `mdbook serve docs`.

It will serve the documentation on http://localhost:3000 with hot reload as you change underlying markdown files.